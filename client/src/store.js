import Vue from "vue";
import Vuex from "vuex";
import ranks from "./store/modules/ranks";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    ranks
  }
});
